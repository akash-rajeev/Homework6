# Sparse Numeric Vector Class

S4 class that represents vectors as sparse vectors, storing only nonzero
values and positions.

## Slots

- `value`:

  nonzero values vector

- `pos`:

  nonzero value positions vector

- `length`:

  integer representing length of vector
